<p id="notice"><%= notice %></p>
<h1>Posts</h1>
<%= link_to 'New Post', posts_new_path %>
<br>
<%= will_paginate @posts %>
<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @posts.each do |f| %>
      <%  unless f == "." || f == ".." %>
        <tr>
          <td><%= f.title %></td>
          <td><%= link_to 'Edit', posts_edit_path(:file => f) %></td>
          <td><%= link_to 'Destroy', posts_destroy_path(:file => f.slug, :id => f.id), data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    <% end %>
  </tbody>
</table>
<%= will_paginate @posts %>
